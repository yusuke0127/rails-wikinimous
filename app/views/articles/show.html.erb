<div class="container">
  <h2><%= @article.title %></h2>
  <p><%= @article.content %></p>
  <%= link_to "Add an article", new_article_path %>
   <!-- < # %= link_to edit_article_path(@article) do %> -->
    <!-- <i class="fas fa-pen"></i> -->
  <%# end %>
  <%= link_to "Edit", edit_article_path %>
  <%= link_to "Delete article", article_path(@article), method: :delete, data: {confirm: "Are you sure you want to delete #{@article.title}"} %>
  <%= link_to "All articles", articles_path %>

</div>
